// API Configuration
// Use relative URL to leverage Vite proxy - works both locally and via tunnels (ngrok/serveo)
// Set VITE_API_URL environment variable to override (e.g., for production)
export const API_URL = import.meta.env.VITE_API_URL || "";

