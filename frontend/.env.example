# Copy to frontend/.env (gitignored). For local OAuth + uploads, point at the API (not the Vite proxy)
# so the session cookie is set on the same host your credentialed fetches use.
VITE_API_BASE_URL=http://localhost:8000
